name: CI/CD Docker

on:
  push:
    branches:
      - main
    paths:
      - '**'

jobs:
  build:
    name: build image
    runs-on: ubuntu-22.04

    steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Check file
          run: ls

        - name: Extract folder name
          id: extract-folder
          run: |
            CHANGED_FOLDER=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep '/' | cut -d'/' -f1 | uniq)
            echo "::set-output name=folder::$CHANGED_FOLDER"
  
        - name: Build Docker image
          run: docker build -t test-image .
  
        # - name: Log in to Docker Hub
        #   run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
  
        # - name: Push Docker image to Docker Hub
        #   run: docker push my-image
  
        # - name: SSH into server and deploy
        #   uses: appleboy/ssh-action@master
        #   with:
        #     host: ${{ secrets.SERVER_IP }}
        #     username: ${{ secrets.SERVER_USERNAME }}
        #     key: ${{ secrets.SERVER_SSH_KEY }}
        #     script: |
        #       docker pull my-image
        #       docker stop my-container || true
        #       docker rm my-container || true
        #       docker run -d --name my-container -p 80:80 my-image
